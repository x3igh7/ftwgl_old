<h1>members</h1>
<div class="content-inner">
<table class="table" cellspacing="0">
  <tr>
    <th>Username</th>
    <th>Teams</th>
  </tr>
  <% @user.each do |user| %>
    <tr>
      <td>
        <%= link_to user.username, user_path(user) %>
      </td>
      <td>
        <% user.teams.each do |team| %>
          <%= link_to "#{team.name} #{team.tag}", team_path(team) %>
        <% end %>
      </td>
    </tr>
  <% end %>
</table>
</div>