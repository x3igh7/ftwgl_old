<div class="container">
  <%= simple_form_for(@team) do |f| %>
    <legend>Edit Team Profile</legend>
    <%= f.error_notification %>
    
    <%= f.input :name %>
    <%= f.input :tag %>
    <%= f.submit "Update Team" %>
  <% end %>
</div>
