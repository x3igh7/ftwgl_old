<h1>quick links</h1>
<div class="content-inner">
	<div class="content-inner">
		<% if user_signed_in? %>
			<%= render 'home_signed_in' %>
		<% else %>
			<%= render 'home_signed_out' %>
		<% end %>
	</div>
</div>
</div>
<div class="spacer"></div>
<div class="content-outer">
<h1>general news</h1>
  <div id="home-news" class="content-inner">
    <%= render 'index_news' %>
  </div>
