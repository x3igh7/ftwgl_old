<% if @screenshot.new_record? %>
  alert("Failed to upload screenshot: <%= j @screenshot.errors.full_messages.join(', ').html_safe %>");
<% else %>
  $('#match-screenshots').append("<%= j link_to @screenshot.image_identifier, @screenshot.image.url.to_s %>");
<% end %>
