 <div class="content-outer comments">
   <div class="content-inner-inner comments-inner">
    <h1>comments</h1>
    <% if @comments.any? %>
        <% @comments.each do |comment| %>
            <%= render :partial => "comment", :locals => { :comment => comment } %>
         <% end %>
    <% else %>
        No comments yet.
     <% end %>
  </div>
</div>
