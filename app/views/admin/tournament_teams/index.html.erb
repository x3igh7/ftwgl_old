<h1>Tournament Teams</h1>
<table>
  <tr>
    <th>Team Name</th>
    <th>Team Tag</th>
    <th></th>
    <th></th>
  </tr>
  <% @teams.each do |team| %>
    <tr>
      <td><%= team.team.name %></td>
      <td><%= team.team.tag %></td>
      <td><%= link_to "edit", edit_admin_tournament_team_path(team) %></td>
      <td><%= link_to "delete", admin_tournament_team_path(team), :method => :delete, :class => 'action destroy', data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>
