/* this script requires events.js, modules.js, and common.js to be included first */
loadTemplates([ "matches", "users", "tournaments", "loading" ]);
Panels.users = $("#users_panel");
Panels.tournaments = $("#tournaments_panel");
Meta.admin = true;
Events.Page.attach();
Events.Users.get();
Events.Tournaments.get();